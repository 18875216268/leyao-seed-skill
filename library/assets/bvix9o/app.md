

# @通用导航库（通用套件索引）

> **框架生态提示**：本资产位于某包的 `library/assets/` 下 → 使用前**先回主框架**：上溯到含 `library/engine.py` 的包根，读其 `SKILL.md` 的〈最高准则〉→ `library/ROUTES.md` → 用户区记忆 / 默认资产卡 → 按 `processor/` 五步执行；**独立安装态**（本目录不在 `library/assets/` 下）不受此限——按本资产独立使用即可。

**这是什么**：一份**资料型资产**——外部可复用技能（PPT / 架构图 / 去 AI 味 / 技能导购等）的清单与获取方式，
供「再找一份可复用技能」时按图索骥；并含 **300 个经典思维模型/分析工具库**（索引 + 精炼）。

**怎么用**：先读本页清单，再按需打开（本资产**不是可执行 Skill**：无脚本、无凭证、不联网、不写任何状态）——
- `公共技能.md` —— 技能清单（作用 / 适用 / 获取地址 / 安装方式）
- `公共经验.md` —— 实测经验账本（取数工具选择 / 凭证 / 口径校验）
- `高频场景指引/` —— 高频场景速查（**场景选择＝路由卡描述**；进入后按其 `app.md` 目录导航**引导读取**：`list` 枚举 → 文件名初筛 → 文件头确认 → 读全文 ✓）
- `经典思维模型/` —— **300 个经典思维模型/工具库**（100 工具×七篇 + 200 框架×三区；索引 + 精炼；SWOT / 波特五力 / 金字塔原理 / MECE / PDCA / AARRR / KANO…；进入后按其 `app.md` 导航）

**边界**：只做「导航」，命中后**原样只读**引用；需要业务数据 → `p3nes3` / `i7c4z1`；需要 GitHub 连通 → `fmcq5n`；
套件内都没有 → 按框架自带判据亲做（并按判据 3.5 提案补资产）。

- `维护.md` —— **能力库蒸馏规范**（经验/场景/技能三产物 × 四门槛 × 提案落地；与卡刷新同一 14:00）
