


# 高频场景指引（目录导航 · 只读）

> **索引止于文件夹级**：场景选择＝路由卡（描述＝触发词，见 `library/ROUTES.md` 本节点及子节点；管理台维护 ✓）。
> **本目录内每个 `.txt`（`app.md` 除外）＝ 一个场景指引**；更细层级**不登记、靠引导读取**（下方四步）✓。

## 引导读取（四步 · 确定性 · 零搜索 ✗）

1. **列目录**：`python library/asset.py list library/assets/bvix9o/高频场景指引/` —— 拿到目录条目与相对路径（**含子目录 → 对其继续 `list`**）；
2. **初筛**：按**文件名**与任务做词面 / 语义比对（文件名自释，含场景名与适用对象；例：`…目标拆解.txt`）——**宁多勿漏**，列出候选；
3. **确认**：打开候选，**先读文件头〈识别与触发〉段**（每个场景文件自带：默认规则 / 示例语句 / 不适用 / 边界）——匹配即命中；不匹配读下一候选（**不整库全读** ✗）；
4. **执行**：命中后读该文件**全文**再动手（⚠️ **未读全文不得动手、不得自称「按方法论执行」** ✗）；流程 / 留证 / 验收 / 歧义询问**严格按主框架**执行——方法只是方法输入，流程照走。

## 维护约定（灵活 · 零登记）

- 新增 / 改名 / 删除场景 ＝ **直接动文件** —— **无需改本页、无需任何索引 / 关联改动** ✓；
- 文件名**自释**；文件头保留〈识别与触发〉段（模式见现有场景文件 ✓）；
- 本页只是导航：**目录＝事实源** —— 任何清单都可能滞后，**以第 1 步 `list` 结果为准** ✓。
