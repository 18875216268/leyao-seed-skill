# 经典思维模型（目录导航 · 只读）

> **本库是什么**：**300 个经典思维模型/分析工具的索引与精炼**——100 个思维工具（战略组织/营销服务/人力资源/质量生产/财务管理/项目物流/决策思维 七篇，源：《100个思维工具详解》1860 页）＋ 200 个经典思维框架（麦肯锡 50 / 经典框架 100 / 营销工具 50，源：《原创思维框架PPT》304 页）。
> **层级**：本页（导航）→ `index.md`（总索引·300 条全量）→ 条目卡（`tools/`）/ 框架精炼（`frameworks/`）/ 方法论（`methods/`）→ `sources.md`（源指针 · 回原文）。

## 引导读取（四步 · 确定性 · 零搜索 ✗）

1. **列目录**：`python library/asset.py list library/assets/bvix9o/经典思维模型/` —— 拿到目录条目（含子目录 → 继续 `list`）；
2. **定位**：读 `index.md`——按词面 / 语义检索条目（300 条全量：名称 + 一句话定位 + 锚点；含"交叉重复映射表"，重名模型一眼对齐）；
3. **取用**：有精炼卡 → 直读对应文件（`tools/<编号>-<名称>.md`、`frameworks/<区>-<序号>-<名称>.md`）；需原文细节 → 按 `sources.md` 锚点回源（**先读全文再动手** ✓；不整库全读 ✗）；
4. **执行**：模型只是**方法输入**——流程 / 留证 / 验收 / 歧义询问**严格按主框架执行**。

## 内容清单（目录＝事实源）

- `index.md` —— 总索引：100 工具（七篇）+ 200 框架（P1/P2/P3）+ 交叉重复映射（20 组）
- `sources.md` —— 源指针：两母本、锚点格式、回源方法（本地素材不随包发布）
- `tools/` —— 100 个工具卡（概念 / 分析步骤 / 特点 / 应用 / 坑 / 锚点）——**批次 2 填充**
- `frameworks/` —— P1/P2/P3 框架精炼（含"什么时候用哪个"选择判据）——**批次 3 填充**
- `methods/` —— 工作方法论精炼（源：赠送方法论 40 个 PPT 去重后）——**批次 4 填充**

## 收录原则

- **去重**：与 `bvix9o/高频场景指引/` 场景指引重复的模型（复盘 / 5Why / 鱼骨 / PDCA / 第一性原理等）→ 只做**指针**、不重复收录；
- **同源合并**：PPT 与工具集同名的 20 组模型（SWOT / PEST / 波特五力 / 波士顿矩阵…）→ 合并为一条、双锚点；
- **分期填充**：索引已全量（批次 1）；精炼正文按批次补充，**填充不改本页**（清单以 `index.md` 与目录 `list` 为准）。

## 边界

- 只做「模型知识」：不取业务数据（→ `p3nes3` / `i7c4z1`）、不做 GitHub 连通（→ `fmcq5n`）、不代替技能获取（→ 父级 `bvix9o` 公共技能清单）；
- 库内未收录 → 按主框架自带判据亲做（可联网补足），交付后提案补条目。
