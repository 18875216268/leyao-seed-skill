# leyao-seed-skill 安装与首次初始化指南

本文件面向**完全没安装过本技能**的用户。里面是一段"安装提示词"，
你只需把自己的包路径 / 宿主填好，整段发给你的 agent（WorkBuddy / CodeBuddy 等），
它会从零帮你安装就位并完成首次初始化。

---

## 怎么用

1. 把下面代码块里 `<...>` 占位符替换成你自己的信息。
2. 整段复制，作为第一条消息发给你的 agent。
3. agent 会自动完成安装、版本自检（含全自动拉取）、每日 14:00 定时任务、就绪戳，并给你一份完成摘要。

---

````markdown
我需要你帮我把 leyao-seed-skill 这个技能框架安装到我的宿主并做首次初始化。
我之前完全没装过，请全程帮我完成；能自动化的不要来问我，只在真正缺信息时问我。

【我的环境信息】
- 宿主：<写 WorkBuddy / CodeBuddy / 具体产品名>
- leyao-seed-skill 包路径：<写包文件夹绝对路径，如 C:\xxx\leyao-seed-skill>
- leyao-seed-core（框架本体）路径：<若有写绝对路径；若没有写"需你从包内或指定源获取">
- 用户区（数据/配置）期望位置：<写绝对路径，或写"默认">

【请按此流程执行】
一、安装就位
1. 把 leyao-seed-skill 装入宿主的技能加载目录（用户级 skills 目录或宿主要求的位置）；
   若宿主要求本体与包分开，则同时就位 leyao-seed-core。
2. 验证宿主已能识别并加载该技能，把加载后的入口 SKILL.md / manifest 路径列给我确认。

二、首次初始化
3. 读 SKILL.md 调用链，再读 library/ROUTES.md 总路由地图，确认"任务先选资产"的调度方式。
4. 执行 version/VERSION.md §零 的"会话首次两项检查"：
   a. 宿主常驻：核对行为 1–4（路由契约 / 卡 / 能力库 / 就绪）在位；
      按 §零 初始化用户区 .leyao-data（data/versions.json、data/state/ready.json 不存在则创建）。
   b. 版本检测与更新：取 GitHub 权威源
      https://github.com/18875216268/leyao-seed-skill 的 manifest 版本，与本地比对——
      · 远端 > 本地 → 全自动拉取：取 staging → 跑预检 run_checks 须全绿 →
        经唯一落地器 evolution/grow.py propose+apply（不需我批准）→ 写版本记录与审计；
      · 远端 ≤ 本地 → 不动、不请示。
   c. 整合去散：扫描宿主技能目录，找出与本资产功能重叠的旧散技能（同名/同用途/已被本资产覆盖者），以本资产为主移除之；
      理由：之前这些技能都是散的，现已统一整合进资产层，留着会让 agent 因"检测到重复"而拒绝执行或行为分叉；
      **仅清功能重叠者，其他用户技能一律保留，绝不波及**。同时确认本资产已全面更新、无旧实现残留（含每日 14:00 定时任务与用户区 .leyao-data 初始化均已刷新到最新）。
5. 确保"每日 14:00 刷新"定时任务在场（已知 id: automation，任务含：卡+能力库+路由契约+就绪）；
   缺失则创建 recurring（FREQ=DAILY;BYHOUR=14;BYMINUTE=0）。
6. 在 ready.json 写就绪戳：date、resident=true、daily_task、当前 version。

三、验证交付
7. 输出"安装完成"摘要：技能加载路径、本地 vs 远端版本、定时任务状态、用户区状态、红线遵守声明。

【红线（不可违反）】
- 安装/同步必须用 UTF-8 安全方式（PowerShell Copy-Item 或宿主原生复制）。
  严禁 robocopy /MIR：它按 GBK 代码页枚举，会静默删除/漏拷中文名文件
  （如 维护.md、公共经验.md、高频场景指引/），让技能装成残缺版。
- 不绕过主框架：任何任务先经 ROUTES.md 选资产，无可用资产才用自带判据。
- 只写包内与用户区 .leyao-data，不碰宿主其他文件；**例外**：整合去散（仅移除与本资产功能重叠的旧散技能）属授权动作，但仅限功能重叠者、绝不及用户其他技能。
- 所有框架更新只走 evolution/grow.py 落地器（propose+apply），无守护进程。
- 自动更新但透明：版本拉取全自动，但结果（覆盖/新增/删除/偏离/备份位置）必须告诉我。
- 预检守门：run_checks 不绿则中止拉取并报原因，绝不静默覆盖。
````

---

## 备注

- 第 4b 步的"全自动拉取"已内置策略，用户无需手动批准升级；但拉取结果必须向用户透明汇报。
- 若你的宿主不是 WorkBuddy / CodeBuddy，把"宿主"一栏写清楚，agent 会按该宿主的技能目录规则就位。
- 本指南随技能包发布；版本随包更新，见 `manifest.json`。
