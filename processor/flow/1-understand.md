# ① 理解

思路不清、资源不备（含**没查过有没有现成方法**），不动手。

## 输入

用户的原始需求（一句话或一段材料）。

## 出口判据（全过才进入规划）

- [ ] 一句话需求复述完成，用户可纠正
- [ ] 关键口径无未处理的 L3 歧义（**列入询问清单即可**；集中询问在规划步）
- [ ] 承接资源已定位（可调资产——**方法论类候选已扫过并留证（有/无命中均记）**；默认资产的读卡见标准调用链首段（未读则先在 ① ⓪ 补读一次；卡 / 索引**不存在或过期**再由 ③ 判据 0.5 处置） / 自带判据 / 需要用户补充的材料——后者与 L3 同列询问清单）
- [ ] 当天首次使用：就绪（1 准则 + 4 行为）已完成，或已按 `version/VERSION.md` 降级 / 提醒

## 动作

1. **就绪检查（仅"每天首次使用"做一次；幂等）**：按 `version/VERSION.md` §零〈1 准则 + 4 行为〉执行就绪——已在位（`ready.json` 为今天）则**静默跳过**；异步、失败降级；结果仅在必要时提醒用户。
2. **复述需求**：用自己的话重述用户要什么，一句话能说清。
3. **口径盘点**：列出关键口径（时间范围、范围主体、计算口径、交付形式），标注每项是"已明确 / 有假设 / 有歧义"。
4. **资源盘点（做法顺序：先问方法、再词面、再语义、列候选）**：
   ⓪ **方法预扫（硬 · 先扫一遍地图，命中与否都记下）**（**只扫地图，不读候选资产的文档**；默认资产卡与能力库索引的读取见标准调用链首段——**未读则先在此补读一次（卡＋索引）**）：读 `library/ROUTES.md`——先看 **★ 默认资产行**，再扫**方法论类节点**（`type=方法论`，或描述标注为方法论／场景指引者，**含子节点**）有没有**与本任务同场景**的现成步骤（口径同 ③ 判据 1a：先词面、再语义；例："整月销售目标拆解"）；**能力库索引已在前置读过**（默认层入口，见 `SKILL.md` 标准调用链）——此处据**卡片描述**做**方法节点匹配**（含子节点）：**未命中时只比对描述 / 场景文件名、不进目录**（省正文）；命中 → 按该目录导航**引导读取**（`asset.py list` → 文件名初筛 → 读文件头〈识别与触发〉确认 → 读全文；**不预载整库、不全读目录** ✗）；无 → 记"无"；命中 → 记下并**纳入规划与执行**（**方法只是方法输入**，取用见 ③ 判据 3.5①），**流程照走**（五步流程 `flow/1~5`、留证、验收一个不省 ✗）；**无论有无命中，都继续**下面的候选圈选；
   在此基础上，拿任务场景逐条对照每个节点的**描述**——
   ① **词面对照**：先拿任务里的**业务名词/别名**对各节点【别名】与【何时用】做一遍字面对照；
   ② **语义对照**：再按**场景整体**对一遍（只按任务类型对，容易漏）；
   ③ **列候选**：把**所有像的**节点列进候选集（含子节点；**宁多勿漏**，筛选留给 ③ 执行步）；候选为空 → 按自带判据亲做；
   ④ **核对输入前置**：每个候选的【输入前置】（凭证/材料）**是否拿得到**——拿不到的先记下（③ 据此降级或排除）；
   ⑤ **记下 id**：候选与依据都要留证（**预扫结果——有/无命中与依据——一并记**；交付步 `grow.py trace --routed` 只用最终命中的 id）。
   **同读用户区记忆 `.leyao-data/data/memory.md`**：命中的「失效模式」要规避，命中的「有效做法」可直接复用（③ 判据 0 执行前会再核一次，两处时点不同）。
5. **歧义分级**（判据自带）：
   - **L1 无关紧要**：任何解读都不影响验收 → 选最常见的，交付时说明假设；
   - **L2 可自行判定**：任务上下文/历史习惯能唯一确定 → 自行判定，交付时显式标注所采用口径；
   - **L3 需用户定**：多解读结果差异大且无法从上下文判定 → 汇总留待规划步集中询问。

## 红旗

- 需求名词未定义（"销售额"是含税还是不含税？）就往下走。
- 没查总路由就开始手搓。
- **有现成方法却自己硬造**（方法预扫扫到了却不用；**经 2.6 举证"不必要"并留证者除外**）。

## 引导（可裁量，非判据）

- **口径盘点的四个常见漏项**（用来自查，不是必答清单）：时间范围（截止时点/时区）、主体范围（谁的数据/含不含下级）、计算口径（含税与否/去重规则/退款取消是否计入）、交付形式（表/图/结论/文件）。
- **L3 的正例**：两种解读会让**交付物本身不同**——如"上月"指自然月还是滚动 30 天、"销售额"含不含税。
- **不是 L3 的情形**：只影响措辞或呈现顺序（→ L1）；上下文或历史习惯已能唯一确定（→ L2，但交付时要显式标注所采用口径）。
- **资源盘点的小技巧**：先拿任务里的**业务名词**去地图描述里对一遍，再按场景整体对一遍——只按任务类型（"查询/汇总"）对，容易漏。
- **思维模型场景提示（可选 · 与方法预扫同一条路）**：任务的**思考类型**落在下面这些场景时，值得在方法预扫里**主动**看一眼 `mtk7r2`（经典思维模型 300）有没有贴合的模型——按场景选用、不硬套（不需要就跳过，守"最小充分"）：分析/归因 → 5W2H · 鱼骨图 · 5Why；决策/取舍 → SWOT · 决策矩阵 · 决策树 · 六顶思考帽；复盘/改进 → PDCA · ORID · 差距分析；拆解/规划 → MECE · 金字塔原理 · 逻辑框架法；优先级 → 时间四象限 · KANO · 责任矩阵；创意/发散 → 头脑风暴 · SCAMPER · 奥斯本检核表法；结构化表达 → 金字塔原理 · MECE。命中后按动作 4⓪ 的导航流程读取，并在规划步落位。
- **先开放式、再收敛**（参考咨询的标准做法）：先用开放问题弄清"开场问题**背后**的真实需求"（你想解决什么 / 给谁看 / 什么时候要），再把它收敛成具体口径选项——**不要一上来就让用户在选项间选**。
